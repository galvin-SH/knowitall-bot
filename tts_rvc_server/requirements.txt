# TTS + RVC Server Dependencies
# Install with: pip install -r requirements.txt

# Core TTS+RVC package (includes edge-tts and RVC inference)
tts-with-rvc>=0.1.9

# Web server
fastapi>=0.104.0
uvicorn>=0.24.0

# Note: PyTorch with CUDA support is installed as a dependency of tts-with-rvc
# If you need a specific CUDA version, install torch manually first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
